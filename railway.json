{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm config set cache /tmp/.npm && npm config set fund false && npm config set audit false && rm -rf package-lock.json node_modules && npm install && cd apps/marketing && rm -rf package-lock.json node_modules && npm install && npm run prebuild && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/marketing && npm run preview -- --host 0.0.0.0 --port $PORT"
  }
}
