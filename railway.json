{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd apps/marketing && npm run prebuild && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/marketing && npm run preview -- --host 0.0.0.0 --port $PORT"
  }
}
