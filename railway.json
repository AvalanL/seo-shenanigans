{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "rm -rf node_modules/.cache || true && npm install --omit=dev --cache /tmp/.npm --prefer-offline --no-audit --no-fund && cd apps/marketing && rm -rf node_modules/.cache || true && npm install --omit=dev --cache /tmp/.npm --prefer-offline --no-audit --no-fund && npm run prebuild && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/marketing && npm run preview -- --host 0.0.0.0 --port $PORT"
  }
}
